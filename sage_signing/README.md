# Sage test signing identity

This folder contains the fixed debug signing identity used by development builds so future APK updates keep the same Android signature. It is for prototype builds only and is not a production release key.
